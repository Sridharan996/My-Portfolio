services:
  - type: web
    name: portfolio-hero-section
    env: static
    buildCommand: npm run build
    publishDir: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
